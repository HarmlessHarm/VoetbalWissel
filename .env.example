# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your-project-url.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Database URL for Drizzle ORM (use DIRECT connection, not pooler)
# Direct connection format: postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
DATABASE_URL=postgresql://postgres:your-db-password@db.your-project-ref.supabase.co:5432/postgres

# Optional: Pooler URL for application queries (faster, but doesn't support migrations)
# DATABASE_URL_POOLER=postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[region].pooler.supabase.com:6543/postgres


# Optional: Service Role Key (keep this secret, only use server-side)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
